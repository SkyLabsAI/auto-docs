{% extends "base.njk" %}

{% block content %}
<h1>{{ title }}</h1>

{% if availableFrom or key %}
<div class="alert alert-info" role="alert">
  Available from {{ (availableFrom or key) | tick | md | safe }}.
</div>
{% endif %}

{{ content | safe }}
{% endblock %}
